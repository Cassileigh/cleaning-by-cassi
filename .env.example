# This project uses Worker bindings, including TURNSTILE_SITE_KEY.
# Copy .dev.vars.example to .dev.vars for local Wrangler configuration.
# No PUBLIC_* build variables or live secrets are needed for mocked tests.
# Never store GITHUB_READ_TOKEN here; configure it as a Cloudflare Build secret.
